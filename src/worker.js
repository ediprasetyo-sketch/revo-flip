const json=(data,status=200)=>new Response(JSON.stringify(data),{status,headers:{'content-type':'application/json'}});export default{async fetch(request,env){const url=new URL(request.url);if(url.pathname==='/api/books'&&request.method==='GET')return json([]);if(url.pathname==='/api/upload/init'&&request.method==='POST')return json({error:'R2 binding belum dikonfigurasi',code:'R2_NOT_READY'},503);if(url.pathname.startsWith('/api/upload/'))return json({error:'Backend upload belum dikonfigurasi'},503);return env.ASSETS.fetch(request)}};