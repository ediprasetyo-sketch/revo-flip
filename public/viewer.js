const params=new URLSearchParams(location.search),id=params.get('id');
const stage=document.querySelector('#bookStage'),book=document.querySelector('#book'),loading=document.querySelector('.flip-loading');
const left=document.querySelector('#leftCanvas'),right=document.querySelector('#rightCanvas'),front=document.querySelector('#frontCanvas'),back=document.querySelector('#backCanvas');
const prev=document.querySelector('#prev'),next=document.querySelector('#next'),label=document.querySelector('#pageLabel');
let pdf,page=1,busy=false,resizeTimer;
function controls(){label.textContent=pdf?`${page} / ${pdf.numPages}`:'– / –';prev.disabled=!pdf||page<=1||busy;next.disabled=!pdf||page>=pdf.numPages||busy}
async function draw(n,canvas,fit){if(!n||n<1||n>pdf.numPages){canvas.width=1;canvas.height=1;canvas.style.width='1px';canvas.style.height='1px';return fit||null}const p=await pdf.getPage(n),base=p.getViewport({scale:1});const maxW=Math.max(220,Math.floor((stage.clientWidth-120)/2)),maxH=Math.max(260,stage.clientHeight-78);const s=Math.max(.2,Math.min(maxW/base.width,maxH/base.height));const v=p.getViewport({scale:s}),r=Math.min(devicePixelRatio||1,2);canvas.width=Math.ceil(v.width*r);canvas.height=Math.ceil(v.height*r);canvas.style.width=`${Math.floor(v.width)}px`;canvas.style.height=`${Math.floor(v.height)}px`;const c=canvas.getContext('2d');c.setTransform(r,0,0,r,0,0);c.clearRect(0,0,v.width,v.height);await p.render({canvasContext:c,viewport:v}).promise;return {w:Math.floor(v.width),h:Math.floor(v.height)}}
function sizeBook(x){book.style.width=`${x.w*2}px`;book.style.height=`${x.h}px`}
async function renderSpread(n){const info=await draw(n,right);sizeBook(info);if(n>1)await draw(n-1,left);else{left.width=1;left.height=1;left.style.width=`${info.w}px`;left.style.height=`${info.h}px`;left.getContext('2d').clearRect(0,0,info.w,info.h)}page=n;controls();loading.hidden=true}
function waitAnimation(){return new Promise(resolve=>{const done=e=>{if(e.target===document.querySelector('#turningPage')){book.removeEventListener('animationend',done);resolve()}};book.addEventListener('animationend',done)})}
async function flip(dir){if(!pdf||busy)return;const target=page+(dir==='next'?1:-1);if(target<1||target>pdf.numPages)return;busy=true;controls();try{const turning=document.querySelector('#turningPage');if(dir==='next'){await draw(page,front);await draw(target,back)}else{await draw(page,left);await draw(target,front);await draw(page,back);turning.style.left='0';turning.style.right='auto'}book.classList.remove('turn-next','turn-prev','is-turning');void book.offsetWidth;book.classList.add('is-turning',dir==='next'?'turn-next':'turn-prev');await waitAnimation();await renderSpread(target)}catch(e){console.error(e);loading.hidden=false;loading.textContent='Gagal membalik halaman PDF.'}finally{document.querySelector('#turningPage').style.left='';document.querySelector('#turningPage').style.right='';book.classList.remove('is-turning','turn-next','turn-prev');busy=false;controls()}}
prev.addEventListener('click',()=>flip('prev'));next.addEventListener('click',()=>flip('next'));
document.querySelector('#share').addEventListener('click',async()=>{try{if(navigator.share)await navigator.share({title:document.title,url:location.href});else{await navigator.clipboard.writeText(location.href);const b=document.querySelector('#share'),t=b.innerHTML;b.innerHTML='✓ <span>Link disalin</span>';setTimeout(()=>b.innerHTML=t,1600)}}catch(e){if(e.name!=='AbortError')alert('Gagal membagikan link.')}});
window.addEventListener('resize',()=>{if(!pdf||busy)return;clearTimeout(resizeTimer);resizeTimer=setTimeout(()=>renderSpread(page),180)});
(async()=>{if(!id){loading.textContent='Flipbook tidak ditemukan.';return}try{pdfjsLib.GlobalWorkerOptions.workerSrc='https://cdnjs.cloudflare.com/ajax/libs/pdf.js/3.11.174/pdf.worker.min.js';pdf=await pdfjsLib.getDocument(`/api/media/${encodeURIComponent(id)}`).promise;await renderSpread(1)}catch(e){console.error(e);loading.textContent='Gagal membuka PDF.'}})();