const MAX_FILE_SIZE=10*1024**3;
const CHUNK_SIZE=10*1024**2;
const RETRIES=3;
const input=document.querySelector('#fileInput'),dropzone=document.querySelector('#dropzone'),info=document.querySelector('#fileInfo'),nameEl=document.querySelector('#fileName'),sizeEl=document.querySelector('#fileSize'),bar=document.querySelector('#progressBar'),progress=document.querySelector('#progressText'),speed=document.querySelector('#speedText'),start=document.querySelector('#startUpload'),status=document.querySelector('#status');
let file;
const key=f=>`revoflip:${f.name}:${f.size}:${f.lastModified}`;
const sleep=ms=>new Promise(r=>setTimeout(r,ms));
function formatSize(n){const u=['B','KB','MB','GB','TB'];let i=0;while(n>=1024&&i<u.length-1){n/=1024;i++}return `${n.toFixed(i?2:0)} ${u[i]}`}
function errorFromResponse(r,fallback){return r.json().then(x=>x.error||fallback).catch(()=>fallback)}
async function blobToBase64(blob){const buffer=await blob.arrayBuffer(),bytes=new Uint8Array(buffer);let binary='';const step=0x8000;for(let i=0;i<bytes.length;i+=step)binary+=String.fromCharCode(...bytes.subarray(i,i+step));return btoa(binary)}
function selectFile(f){if(!f)return;if(f.type&&f.type!=='application/pdf'&&!/\.pdf$/i.test(f.name)){status.textContent='Hanya file PDF yang didukung.';return}if(f.size>MAX_FILE_SIZE){status.textContent='Ukuran file melebihi batas 10 GB.';return}file=f;nameEl.textContent=f.name;sizeEl.textContent=formatSize(f.size);info.hidden=false;status.textContent=localStorage.getItem(key(f))?'Sesi upload sebelumnya ditemukan. Klik Mulai Upload untuk melanjutkan.':'Siap diunggah.'}
async function retry(fn){let last;for(let i=0;i<RETRIES;i++){try{return await fn()}catch(err){last=err;if(i<RETRIES-1)await sleep(700*(i+1))}}throw last}
async function waitUntilReady(id){let attempt=0;while(true){attempt++;const r=await fetch(`/api/books/${encodeURIComponent(id)}/status`,{cache:'no-store'});if(!r.ok)throw new Error(await errorFromResponse(r,'Status processing tidak tersedia'));const data=await r.json();if(data.status==='ready')return data;if(data.status==='error')throw new Error(data.processing_error||'PDF gagal diproses di server');const dots='.'.repeat((attempt%3)+1);progress.textContent='Upload selesai — memproses PDF';bar.style.width='100%';speed.textContent=`Menyiapkan halaman pertama${dots}`;status.textContent='PDF sedang dioptimalkan dan cover sedang dibuat. Jangan tutup halaman ini.';await sleep(2000)}}
input.addEventListener('change',e=>selectFile(e.target.files[0]));['dragenter','dragover'].forEach(t=>dropzone.addEventListener(t,e=>{e.preventDefault();dropzone.classList.add('dragging')}));['dragleave','drop'].forEach(t=>dropzone.addEventListener(t,e=>{e.preventDefault();dropzone.classList.remove('dragging')}));dropzone.addEventListener('drop',e=>selectFile(e.dataTransfer.files[0]));
start.addEventListener('click',async()=>{if(!file)return;start.disabled=true;const storageKey=key(file),started=performance.now();try{let session=JSON.parse(localStorage.getItem(storageKey)||'null');if(!session){const r=await fetch('/api/upload/init',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({name:file.name,size:file.size,type:'application/pdf',chunkSize:CHUNK_SIZE})});if(!r.ok)throw new Error(await errorFromResponse(r,'Gagal membuat sesi upload'));session=await r.json();localStorage.setItem(storageKey,JSON.stringify(session))}const chunkSize=Number(session.chunkSize)||CHUNK_SIZE,st=await fetch(`/api/upload/${encodeURIComponent(session.uploadId)}/status`);if(!st.ok){localStorage.removeItem(storageKey);throw new Error(await errorFromResponse(st,'Sesi upload tidak tersedia'))}const state=await st.json(),done=new Set(state.parts.map(Number)),totalParts=Math.ceil(file.size/chunkSize);let uploaded=0;for(const part of done){const off=(part-1)*chunkSize;uploaded+=Math.max(0,Math.min(chunkSize,file.size-off))}for(let part=1;part<=totalParts;part++){if(done.has(part))continue;const offset=(part-1)*chunkSize,chunk=file.slice(offset,Math.min(offset+chunkSize,file.size),'application/octet-stream');await retry(async()=>{const data=await blobToBase64(chunk),r=await fetch(`/api/upload/part?uploadId=${encodeURIComponent(session.uploadId)}&part=${part}`,{method:'PUT',headers:{'content-type':'application/json'},body:JSON.stringify({data})});if(!r.ok)throw new Error(await errorFromResponse(r,`Part ${part} gagal (${r.status})`))});uploaded+=chunk.size;const pct=uploaded/file.size*100;bar.style.width=`${pct}%`;progress.textContent=`${pct.toFixed(1)}% — bagian ${part}/${totalParts}`;const secs=(performance.now()-started)/1000;speed.textContent=`${formatSize(uploaded/Math.max(secs,.1))}/s`}const complete=await fetch('/api/upload/complete',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({uploadId:session.uploadId,title:file.name.replace(/\.pdf$/i,'')})});if(!complete.ok)throw new Error(await errorFromResponse(complete,'Gagal menyelesaikan upload'));const result=await complete.json();localStorage.removeItem(storageKey);bar.style.width='100%';progress.textContent='Upload selesai — memulai processing';speed.textContent='Mengoptimalkan PDF…';status.textContent='File berhasil diupload. Menunggu PDF siap dibuka…';await waitUntilReady(result.id);progress.textContent='PDF siap — membuka flipbook';speed.textContent='Halaman pertama siap';status.textContent='Membuka flipbook…';await sleep(300);location.href=result.viewer}catch(err){status.textContent=`Upload/processing terhenti: ${err.message}.`;speed.textContent='';start.disabled=false}});