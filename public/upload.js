const MAX_FILE_SIZE=10*1024**3;const CHUNK_SIZE=50*1024**2;const input=document.querySelector('#fileInput'),dropzone=document.querySelector('#dropzone'),info=document.querySelector('#fileInfo'),nameEl=document.querySelector('#fileName'),sizeEl=document.querySelector('#fileSize'),bar=document.querySelector('#progressBar'),progress=document.querySelector('#progressText'),speed=document.querySelector('#speedText'),start=document.querySelector('#startUpload'),status=document.querySelector('#status');let file;function formatSize(n){const u=['B','KB','MB','GB','TB'];let i=0;while(n>=1024&&i<u.length-1){n/=1024;i++}return `${n.toFixed(i?2:0)} ${u[i]}`}function selectFile(f){if(!f)return;if(f.type!=='application/pdf'){status.textContent='Hanya file PDF yang didukung.';return}if(f.size>MAX_FILE_SIZE){status.textContent='Ukuran file melebihi batas 10 GB.';return}file=f;nameEl.textContent=f.name;sizeEl.textContent=formatSize(f.size);info.hidden=false;status.textContent='Siap diunggah.'}input.addEventListener('change',e=>selectFile(e.target.files[0]));['dragenter','dragover'].forEach(t=>dropzone.addEventListener(t,e=>{e.preventDefault();dropzone.classList.add('dragging')}));['dragleave','drop'].forEach(t=>dropzone.addEventListener(t,e=>{e.preventDefault();dropzone.classList.remove('dragging')}));dropzone.addEventListener('drop',e=>selectFile(e.dataTransfer.files[0]));start.addEventListener('click',async()=>{if(!file)return;status.textContent='Menghubungi server upload...';start.disabled=true;const started=performance.now();try{const init=await fetch('/api/upload/init',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({name:file.name,size:file.size,type:file.type,chunkSize:CHUNK_SIZE})});if(!init.ok)throw new Error('Gagal membuat sesi upload');const session=await init.json();let uploaded=0;for(let offset=0,part=1;offset<file.size;offset+=CHUNK_SIZE,part++){const chunk=file.slice(offset,Math.min(offset+CHUNK_SIZE,file.size));const r=await fetch(`/api/upload/part?uploadId=${encodeURIComponent(session.uploadId)}&part=${part}`,{method:'PUT',body:chunk});if(!r.ok)throw new Error(`Part ${part} gagal`);uploaded+=chunk.size;const pct=uploaded/file.size*100;bar.style.width=`${pct}%`;progress.textContent=`${pct.toFixed(1)}% — ${part} bagian selesai`;const secs=(performance.now()-started)/1000;speed.textContent=`${formatSize(uploaded/Math.max(secs,.1))}/s`}const done=await fetch('/api/upload/complete',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({uploadId:session.uploadId,name:file.name,size:file.size,type:file.type})});if(!done.ok)throw new Error('Gagal menyelesaikan upload');const result=await done.json();status.innerHTML=`Upload berhasil. <a href="/viewer.html?id=${encodeURIComponent(result.id||'')}">Buka Flipbook</a>`}catch(err){status.textContent=`Upload gagal: ${err.message}. Progress dapat digunakan untuk mekanisme resume setelah backend aktif.`}finally{start.disabled=false}});